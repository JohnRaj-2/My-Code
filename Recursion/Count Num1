#include<bits/stdc++.h>
using namespace std;

int i=1;
void prints(int i,int n){
    if(i>n)
    return;

    cout<< i << endl;
    prints(i+1,n);
}


int main(){
    cout << "How many numbers you want to print? :";
    int n;
    cin >> n;
    prints(i,n);
}
